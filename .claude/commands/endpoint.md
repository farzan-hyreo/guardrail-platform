---
description: Add one operation to an existing resource
---
Add this operation: $ARGUMENTS

Use the `the-block` skill. Add the operation to the resource in the registry, add its
contract, add the handler in the owning service, then one line in the gateway router.
Nothing else. Run `pnpm verify`.
