---
description: Trace a request or diagnose a failing call across the bus
---
Diagnose: $ARGUMENTS

Use the `runbook` and `jetstream-service` skills. Identify which of the eleven gates or
which hop failed before proposing a fix. Report the gate by name and the evidence, then
the smallest change that fixes it.
